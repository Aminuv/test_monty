#include "monty.h"

/**
 * nop - push the data in the stack or queue
 * @stack: pointer to the stack
 * @line_number: instruction
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
